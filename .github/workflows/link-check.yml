name: Link Check
on:
  pull_request:
    paths:
      - '**/*.md'
      - 'site/**/*.html'
jobs:
  lychee:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: lycheeverse/lychee-action@v2
        with:
          args: --no-progress --exclude-mail --max-redirects 5 --accept 200,206,429 --retry-wait-time 2 --retry-count 2 .
